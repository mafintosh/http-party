{
  "name": "http-party",
  "version": "1.1.0",
  "description": "Run multiple http servers that share a port file and does automatic failover if the server listening on the port crashes",
  "repository": "mafintosh/http-party",
  "dependencies": {
    "freeport": "^1.0.2",
    "once": "^1.3.0"
  }
}
